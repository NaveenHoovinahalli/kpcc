<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <teli.com.kpcc.views.CcTextView
        style="@style/text_view"
        android:text="SENIOR CONGRESS LEADER"
        android:textColor="@android:color/white"
        android:background="#F7941E"
        android:layout_marginTop="5dp"
        android:id="@+id/cctvVideo"/>

    <GridView
        android:id="@+id/grid_videos_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:numColumns="3"
        android:padding="2dp"
        android:layout_below="@+id/cctvVideo"
        android:horizontalSpacing="2dp"
        android:verticalSpacing="2dp"
       />

    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"/>
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/framelayoutvideo"
        android:layout_centerInParent="true"
        android:visibility="invisible"
        />

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:id="@+id/fragmentofmenu"
        android:background="#000000"
        android:layout_alignParentRight="true"
        android:visibility="invisible"
        />

</RelativeLayout>